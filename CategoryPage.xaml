<Page x:Class="ResearchManagerNotebook.CategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="CategoryPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GridSplitter HorizontalAlignment="Right"  Width="4"/>

        <Label Content="Title" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,12,12,0" Name="lblTitle" Grid.Column="0"/>
        <ScrollViewer Grid.Column="0"  Margin="0,36,0,0">
            <StackPanel>
                <Label Content="Note 1" Margin="12,12,12,0"/>
            </StackPanel>
        </ScrollViewer>
        
        <Label Content="Comments" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,12,12,0" Name="lblComment" Grid.Column="1"/>
        <TextBox TextWrapping="Wrap" Text="note comment" Margin="12,36,12,12" Name="txtComment" Grid.Column="1"/>
    </Grid>
</Page>
